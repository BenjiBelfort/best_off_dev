const Separator = () => {
  return (
    <div
      className="w-[90%] md:w-[70%] h-[2px] mx-auto my-8"
      style={{
        background: 'linear-gradient(to right, transparent, #f87171, transparent)'
      }}
    />
  );
};

export default Separator;
